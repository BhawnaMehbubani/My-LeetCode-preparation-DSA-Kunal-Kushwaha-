class Solution {
    public int[] buildArray(int[] nums) {
        int l=nums.length;
        int[] n=new int[l];
        for (int i=0;i<l;i++)
        {
            n[i]=(nums[nums[i]]);
        }
        return n;
    }
}